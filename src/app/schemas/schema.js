const shemindex = require('./shemindex');
const shemnavigation = require('./shemnavigation');
const announcements = require('./announcement');

const schema = {
    shemindex,
    shemnavigation,
    announcements
}

module.exports = schema;